language: node_js

node_js:
  - "10.13.0"

sudo: true

env:
  - CXX=g++-4.8 NODE_ENV=testing DEPLOYED=0
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-4.8
install:
  - ln -sfn /home/node/node_modules node_modules