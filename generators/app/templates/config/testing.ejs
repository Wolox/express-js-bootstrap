exports.config = {
  environment: 'testing',
  isTesting: true,
  common: {<% if(orm.sequelize || orm.mongoose) {%>
    database: {
      name: process.env.DB_NAME_TEST
    },
    <%}%>
    session: {
      secret: 'some-super-secret'
    }
  }
};
