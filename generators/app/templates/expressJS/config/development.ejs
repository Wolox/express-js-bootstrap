exports.config = {
  environment: 'development',<% if(database && (orm.sequelize || orm.mongoose)) {%>
  common: {
    database: {
      name: process.env.DB_NAME_DEV
    }
  }, <%}%>
  isDevelopment: true
};
