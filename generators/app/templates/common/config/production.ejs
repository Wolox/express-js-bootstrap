exports.config = {
  environment: 'production',<% if(database && (orm.sequelize || orm.mongoose)) {%>
  common: {
    database: {
      name: process.env.DB_NAME
    },
    <% if(technology === 'graphQL') {%>
      redisCache: {
        name: process.env.REDIS_NAME_DEV
      }
      <%}%>
  }, <%}%>
  isProduction: true
};
